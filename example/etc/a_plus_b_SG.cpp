#include <bits/stdc++.h>

#define long long long

using namespace std;

struct test {
    test *p;
};

long a, b;
test *ptr;

int main() {
    scanf("%lld %lld", &a, &b);
    printf("%lld\n", a + b);
    a = (long) (ptr->p->p);

    return 0;
}
